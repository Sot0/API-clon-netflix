type Query {
    getUsers: [User]!
    getUserById(id: ID!): User!
    getMovies: [Movie]!
    getMovieById(id: ID!): Movie!
}

type Mutation {
    createUser(data: UserCreateInput!): User!
    updateUser(id: ID!, data: UserUpdateInput!): User!
    deleteUser(id: ID!): User!
    createMovie(data: MovieCreateInput!): Movie!
    updateMovie(id: ID!, data: MovieUpdateInput!): Movie!
    deleteMovie(id: ID!): Movie!
    createCommentonByMovieId(id:ID!, data: CommentCreateInput!): Movie!
}

type User {
    _id: ID!,
    first_name: String!
    last_name: String!
    email: String!
    birth_date: String!
    gender: GENDERS
    profile_pic: String!
    favorites: [Movie]!
    is_active: Boolean!
}

type Movie {
    _id: ID!
    title: String!
    author: String!
    description: String!
    video: String!
    cover: String!
    comments: [Comment]!
    is_active: Boolean!
}

type Comment {
    user: User!
    text: String!
}

enum GENDERS {
    M
    F
}

input UserCreateInput {
    first_name: String!
    last_name: String!
    email: String!
    birth_date: String!
    gender: GENDERS!
    profile_pic: String
    is_active: Boolean
}

input UserUpdateInput {
    first_name: String
    last_name: String
    email: String
    birth_date: String
    gender: GENDERS
    profile_pic: String
    favorites: [ID]
    is_active: Boolean
}

input MovieCreateInput {
    title: String!
    author: String!
    description: String!
    video: String!
    cover: String!
    is_active: Boolean!
}

input MovieUpdateInput {
    title: String
    author: String
    description: String
    video: String
    cover: String
    comments: [CommentCreateInput]
    is_active: Boolean
}

input CommentCreateInput {
    user: ID!
    text: String!
}